# -*- mode: snippet -*-
# name: spec
# key: spec
# expand-env: ((yas-indent-line 'fixed) (yas-wrap-around-region 'nil))
# --
require "rails_helper"

describe ${`(jedi/ruby-current-module-name)`} do
  it "does something" do
    expect(true).to be_truthy$1
  end
end